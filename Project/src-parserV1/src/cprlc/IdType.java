package cprlc;

/**
 * The types for identifiers stored in the identifier table.
 */
public enum IdType
  {
    constantId,
    variableId,
    arrayTypeId,
    enumTypeId,
    enumConstId,
    recordTypeId,
    fieldId,
    procedureId,
    functionId;
  }
